# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# --- Dossiers : les résultats (releve.xlsx, ecart.md, .qpl, PDF annotés) SONT dans le dépôt — c'est la preuve
# demandée par Francis (plans et devis = appels d'offres publics, 2026-09-21). Restent hors dépôt :
# les relevés humains scannés (prix et heures internes de DR), les rasters Plan Expert (volume) et les PDF en double.
dossiers/*/entree/reference-*.pdf
dossiers/*/planexpert/*.png
dossiers/*/Dossier-complet.pdf

# --- Secrets : clés, tokens, mots de passe, licences ------------------------
.env
*.key
*.pem
*_rsa
*_ed25519
authorized_keys
*.private.json
credentials.local.env
*token*
*secret*

# --- Images de VM, installateurs, binaires sous licence ---------------------
*.vhdx
*.vmdk
*.qcow2
*.img
*.iso
*.msi
storage/
oem/PlanExpert/

# --- Archives volumineuses ---------------------------------------------------
*.zip
*.7z
*.tar.gz
dossiers/*/export-natif/[sS]-1???.pdf
